1、table里面最好不要对tr进行操作，th和td不能在一个tr里面，
   td的width设置可以直接写无需另加在样式表里，
   弄清楚table的默认设置，避免多余的设置
2、先弄清楚接口文档里的数据是如何写的，不能对数据进行更改分组，根据判断来显示不同的样式
3、返回上一页的跳转尽量不用链接，而是根据浏览器的history来进行
4、跳转到详情页的链接不能仅加在文字或者图标上，要加在整体
5、移动端布局用flex布局写，避免使用float
6、简化样式简化样式！！！页面整体布局前好好思考，怎样最简单！！！